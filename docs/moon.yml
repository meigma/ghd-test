language: 'typescript'
layer: 'application'
stack: 'frontend'
tags:
  - 'docs'
  - 'docusaurus'

project:
  title: 'ghd-test docs'
  description: 'Small documentation site for the ghd-test release fixture.'
  owner: 'meigma'
  maintainers:
    - 'meigma'

toolchains:
  default: 'node'

fileGroups:
  configs:
    - 'docusaurus.config.ts'
    - 'tsconfig.json'
    - 'package.json'
    - 'package-lock.json'
  sources:
    - 'docs/**/*'
    - 'src/**/*'
    - 'static/**/*'

# Moon will infer tasks like build, start, serve, and typecheck from
# package.json scripts because javascript.inferTasksFromScripts is enabled
# in /.moon/toolchains.yml.
#
# Add explicit tasks here when you need custom inputs, outputs, or
# cross-project dependencies.
#
# tasks:
#   build-static:
#     command: 'npm run build'
#     toolchains: ['javascript', 'node', 'npm']
#     inputs:
#       - '@group(configs)'
#       - '@group(sources)'
#     outputs:
#       - 'build/**/*'
